{
  "watch": ["src/", "server.ts"],
  "ext": "ts,tsx,js,jsx,json",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "npx tsx server.ts",
  "events": {
    "start": "node -e \"console.clear()\"",
    "crash": "node -e \"console.error('Nodemon crashed. Waiting for changes...')\""
  }
}